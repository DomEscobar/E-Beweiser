import * as React from 'react';
import { BoxProps } from '../box';
interface IFormControlBase {
    children?: React.ReactNode;
    isInvalid?: boolean;
    isRequired?: boolean;
    isDisabled?: boolean;
    isReadOnly?: boolean;
}
export declare type FormControlProps = IFormControlBase & BoxProps;
export {};
